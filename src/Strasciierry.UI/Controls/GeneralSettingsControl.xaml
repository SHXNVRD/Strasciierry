<?xml version="1.0" encoding="utf-8"?>
<base:UserControlBase x:Class="Strasciierry.UI.Controls.GeneralSettingsControl"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:local="using:Strasciierry.UI.Controls"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:base="using:Strasciierry.UI.Controls"
                      xmlns:controls="using:CommunityToolkit.WinUI.Controls"
                      xmlns:xaml="using:Microsoft.UI.Xaml"
                      mc:Ignorable="d">

    <ScrollViewer>
        <Grid>
            <StackPanel Margin="56,44">
                <controls:SettingsExpander x:Uid="Settings_Theme">

                    <controls:SettingsExpander.HeaderIcon>
                        <FontIcon Glyph="&#xE790;" />
                    </controls:SettingsExpander.HeaderIcon>

                    <controls:SettingsExpander.Items>
                        <controls:SettingsCard ContentAlignment="Left">
                            <RadioButtons>
                                <RadioButton x:Uid="Settings_Theme_Light"
                                             IsChecked="{x:Bind ViewModel.GeneralSettings.AppTheme, Converter={StaticResource ThemeToBooleanConverter}, ConverterParameter=Light, Mode=TwoWay}">
                                    <RadioButton.CommandParameter>
                                        <xaml:ElementTheme>Light</xaml:ElementTheme>
                                    </RadioButton.CommandParameter>
                                </RadioButton>
                                <RadioButton x:Uid="Settings_Theme_Dark"
                                             IsChecked="{x:Bind ViewModel.GeneralSettings.AppTheme, Converter={StaticResource ThemeToBooleanConverter}, ConverterParameter=Dark, Mode=TwoWay}">
                                    <RadioButton.CommandParameter>
                                        <xaml:ElementTheme>Dark</xaml:ElementTheme>
                                    </RadioButton.CommandParameter>
                                </RadioButton>
                                <RadioButton x:Uid="Settings_Theme_Default"
                                             IsChecked="{x:Bind ViewModel.GeneralSettings.AppTheme, Converter={StaticResource ThemeToBooleanConverter}, ConverterParameter=Default, Mode=TwoWay}">
                                    <RadioButton.CommandParameter>
                                        <xaml:ElementTheme>Default</xaml:ElementTheme>
                                    </RadioButton.CommandParameter>
                                </RadioButton>
                            </RadioButtons>
                        </controls:SettingsCard>
                    </controls:SettingsExpander.Items>
                </controls:SettingsExpander>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</base:UserControlBase>
